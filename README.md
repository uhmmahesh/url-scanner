# url-scanner
we build a URL scanner to heuristic security analysis on web addresses. It uses a cyberpunk-inspired interface to simulate a high-tech security terminal that evaluates the risk of a URL without ever actually visiting the site.
